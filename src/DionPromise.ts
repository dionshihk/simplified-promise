type Status = "RESOLVED" | "REJECTED" | "PENDING";

// type ConstructorArg = ?
// type ResolveFn = ?
// type RejectFn = ?
// type OnResolveFn = ?
// type OnRejectFn = ?

export class DionPromise<T> {
    constructor() {}
}
